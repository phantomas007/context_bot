###> prod environment ###
APP_DEBUG=0

# ВАЖНО: APP_SECRET и DATABASE_URL ДОЛЖНЫ быть заданы через:
#   - реальные переменные окружения (docker-compose environment:)
#   - либо файл .env.prod.local (НЕ добавлять в git!)
# Пример: cp .env.prod.local.example .env.prod.local
###< prod environment ###
